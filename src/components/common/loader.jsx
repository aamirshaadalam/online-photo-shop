import React from 'react';

const Loader = () => {
  return (
    <div data-testid='loader' className='loader-container'>
      <svg className='loader' viewBox='0 0 24 24'></svg>
    </div>
  );
};

export default Loader;
